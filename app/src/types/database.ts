// Este archivo es generado por `pnpm supabase gen types typescript`.
// Reemplazar en Fase 1 con el output real.
// Por ahora es un placeholder para que TypeScript compile.

export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
